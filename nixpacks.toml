[phases.setup]
nixPkgs = ["nodejs-20_x", "python3", "gcc", "gnumake"]

[phases.install]
cmds = [
  "python3 --version",
  "pnpm install"
]

[phases.build]
cmds = ["pnpm run build"]

[start]
cmd = "pnpm start"

