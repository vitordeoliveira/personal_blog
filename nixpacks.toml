[phases.setup]
nixPkgs = ["python3", "gcc", "gnumake"]

[phases.install]
cmds = [
  "python3 --version",
  "npm install -g pnpm",
  "pnpm install"
]

[phases.build]
cmds = ["pnpm run build"]

[start]
cmd = "pnpm start"

