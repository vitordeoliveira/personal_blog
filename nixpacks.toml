[phases.setup]
nixPkgs = ["nodejs_24", "python3", "gcc", "gnumake"]

[phases.install]
cmds = [
  "python3 --version",
  "pnpm install"
]

[phases.build]
cmds = ["pnpm run build"]

[start]
cmd = "pnpm start"

